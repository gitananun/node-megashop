<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container px-4 px-lg-5">
        <a class="navbar-brand" href="/">MegaShop</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span
                class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
                <!-- Routes -->
                <li class="nav-item">
                    <a class="nav-link <%= path == '/' ? 'active' : '' %>" aria-current="page" href="/">
                        Store
                    </a>
                </li>

                <!-- Start Admin Routes -->
                <% if(authenticated) { %>
                    <li class="nav-item"><a class="nav-link <%= path == '/admin/add-product' ? 'active' : '' %>"
                            href="/admin/add-product">
                            Add Product
                        </a>
                    </li>
                    <li class="nav-item"><a class="nav-link <%= path == '/admin/products' ? 'active' : '' %>"
                            href="/admin/products">
                            Admin Products
                        </a>
                    </li>
                    <% } %>
                        <!-- End Admin Routes -->

                        <!-- Start Auth Routes -->
                        <% if(!authenticated) { %>
                            <li class="nav-item"><a class="nav-link <%= path == '/login' ? 'active' : '' %>"
                                    href="/login">
                                    Login
                                </a>
                            </li>
                            <% } %>
                                <!-- End Auth Routes -->
            </ul>
            <a class="d-flex text-decoration-none" href="/cart">
                <button class="btn btn-outline-dark <%= path == '/cart' ? 'btn-dark text-light' : '' %> " type="submit">
                    <i class="bi-cart-fill me-1"></i>
                    Cart
                </button>
            </a>
            <a class="d-flex px-2 text-decoration-none" href="/orders">
                <button class="btn btn-outline-primary <%= path == '/cart' ? 'btn-dark text-light' : '' %> "
                    type="submit">
                    <i class="bi-app-indicator me-1"></i>
                    Orders
                </button>
            </a>
        </div>
    </div>
</nav>
